# Build and local output
/build/
/bin/
/tmp/
/memos
/memos.exe
coverage.out
/.pnpm-store/
/.worktrees/

# Not used by the backend-only image build
# /web/ is no longer excluded — needed for the full Dockerfile
# that builds both frontend and backend.
/docs/
/.github/
*.md
/.gitignore
/.dockerignore
/.golangci.yaml

# Local metadata and secrets
/.git/
/.env
/.env.*
/.idea/
/.vscode/
/.claude/
**/.DS_Store
